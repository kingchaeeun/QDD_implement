/**
 * PostCSS configuration for Vite (ESM compatible).
 * Using `export default` because `package.json` has `"type": "module"`.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
